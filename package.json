{
  "name": "aves-xr-gallery",
  "version": "1.0.0",
  "description": "Galería interactiva de aves venezolanas con soporte AR (WebXR / Scene Viewer / Quick Look)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@google/model-viewer": "^4.0.0"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.5.0",
    "@gltf-transform/extensions": "^4.5.0",
    "gh-pages": "^6.3.0",
    "sharp": "^0.35.4",
    "vite": "^6.3.5"
  }
}
